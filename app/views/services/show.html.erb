<% content_for :title do %><%= @service.name %> | <% end %>

<% content_for :header do %>
    <% unless @service.approved? %>
        <div class="notice notice--pending">
            <div class="notice__body">
                <strong><p>This service has pending changes</p></strong>
                <p>A council staff member will review and publish these changes soon. You can still make more changes in the meantime.</p>    
            </div>
        </div>
    <% end %>

    <%= link_to "Go back", organisations_path, class: "go-back" %>
    <h1 class="page-header__title"><%= @service.name %></h1>
<% end %>


<%= form_with model: @service, local: true, html: {class: "two-thirds"} do |f| %>

    <%= render "shared/errors", model: @service %>

    <section class="community-editor">
        <div class="field-section field-section--two-cols">
            <div class="field">
                <%= f.label :name, class: "field__label" %>
                <p class="field__hint">This will be publicly visible</p>
                <%= f.text_field :name, value: @service.name, class: "field__input" %>
            </div>

            <div class="field field--span-two-cols">
                <%= f.label :description, "Descripton", class: "field__label" %>
                <p class="field__hint">Describe what you do in a short paragraph</p>
                <%= f.text_area :description, value: @service.description, class: "field__input", rows: 4 %>
            </div>

            <div class="field">
                <%# f.label :email, class: "field__label" %>
                <%# f.text_field :email, value: @service.email, class: "field__input" %>
            </div>

            <div class="field">
                <%= f.label :url, "Website", class: "field__label" %>
                <%= f.text_field :url, value: @service.url, class: "field__input" %>
            </div>
        </div>
    </section>

    <section class="community-editor">
        <h2 class="community-editor__title">Contacts</h2>
        <p class="community-editor__hint">Add individuals who members of the public can contact.</p>
        <%= render "admin/services/editors/contacts", s: f %>
    </section>

    <section class="community-editor">
        <h2 class="community-editor__title">Categorisation</h2>
        <p class="community-editor__hint">Putting your service in the right categories helps people find it. Select all that apply, but be specific.</p>
        <%= render "admin/services/editors/taxonomies", s: f %>
    </section>

    <section class="community-editor">
        <h2 class="community-editor__title">Location</h2>
        <p class="community-editor__hint">Add places where people can access your service. Normally these are venues. Approximate addresses are fine. If your service covers the whole county, leave this blank.</p>
        <%= render "admin/services/editors/locations", s: f %>
    </section>

    <section class="community-editor">
        <h2 class="community-editor__title">Local offer and SEND</h2>
        <p class="community-editor__hint">Help people understand how you support those with special educational needs or disabilities.</p>
    </section>

    <div class="form-actions">
        <%= f.submit "Submit for review", class: "button" %>
        <%= link_to "Remove", service_path(@service), method: "delete", class: "button button--danger", data: {confirm: "Are you sure? All details associated with this service will be lost and it won't be visible to the public any more." } %>
    </div>

<% end %>