<header class="site-header">
    <div class="<%= "container" unless full_width %> site-header__inner">
        <div class="site-header__masthead">
            <%= link_to image_tag("logo.svg", alt: "Buckinghamshire Council", class: "site-header__logo"), "https://buckinghamshire.gov.uk", class: "site-header__logo-link" %>
        </div>
        <% if user_signed_in? %>
        <div class="site-header__actions">
            <%= render "shared/app-switcher" %>
            <%= link_to current_user.display_name, edit_user_registration_path, class: "site-header__username"%>
            <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "site-header__link" %>
        </div>
        <% end %>
        <nav class="site-header__service-name-holder">
            <%= link_to "Family Information Service", root_path, class: "site-header__service-name"%>
            <% if current_user && current_user.admin? %>
            <strong class="site-header__phase-tag">Beta</strong>
            <% end %>
        </nav>
    </div>
</header>