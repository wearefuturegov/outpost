<% content_for :header do %>
    <%= render "shared/dynamic-back-link", text: "Back to accessibilities", path: admin_accessibilities_path %>
    <div class="page-header__actions">
      <h1 class="page-header__title two-thirds">
        Add new accessibility option
      </h1>
    </div>
<% end %>

<%= form_for [:admin, @accessibility], html: {class: "one-half"}, data: {warn_unsaved_changes: true} do |f| %>
  <%= render "shared/errors", model: @accessibility %>
  <div class="field-section">
    <%= render "fields", f: f %>
  </div>
  <div class="form-actions">
    <%= f.submit "Create", class: "button" %>
  </div>
<% end %>