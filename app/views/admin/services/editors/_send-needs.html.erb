<%= s.collection_check_boxes( :send_need_ids, SendNeed.all, :id, :name) do |c| %>
    <div class="field checkbox">
        <%= c.check_box class: "checkbox__input" %>
        <%= c.label class: "checkbox__label" %>
    </div>
<% end %>